apiVersion: v1
kind: Service
metadata:
  name: my-service10
  labels: {
    "showOnClusterPortal" : "false"
  }
spec:
  selector:
    app: MyApp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 1234
